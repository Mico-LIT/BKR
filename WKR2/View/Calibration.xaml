<Window x:Class="WKR2.View.Calibration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WKR2.View"
        mc:Ignorable="d"
        Title="Calibration" Height="235.649" Width="300">
    <Grid Name="so">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="177*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock HorizontalAlignment="Center">X</TextBlock>
        <TextBlock HorizontalAlignment="Center" Grid.Column="1">Y</TextBlock>
        <TextBox Name="X" Grid.Row="1" Height="20" VerticalAlignment="Top" Text="{Binding Path=X ,Mode=TwoWay,UpdateSourceTrigger =PropertyChanged}" ></TextBox>
        <TextBox Name="Y" Grid.Row="1" Grid.Column="1" Height="20" VerticalAlignment="Top" Text="{Binding Path=Y ,Mode=TwoWay,UpdateSourceTrigger = PropertyChanged }"></TextBox>
    </Grid>
</Window>
